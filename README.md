# Student-Management-System
The Student Management System is a CLI-based Python application used to manage student records. It performs basic CRUD operations (Create, Read, Update, Delete) and stores data permanently using a JSON file.
